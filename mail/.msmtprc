logfile ~/.msmtp.log

auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt

# Default account.
host     smtp.office365.com 
port     587
from     <from>
user     <user>
password <password>
